version: "3.8"

services:
  orchesis:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./config:/app/config
    environment:
      - ORCHESIS_LOG_LEVEL=info
    restart: unless-stopped
