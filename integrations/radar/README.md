# orchesis-radar
Radar visualization for your AI agent overwatch.

## Usage
`npx orchesis-radar`

Opens `http://localhost:8081` with:
- Demo mode if Orchesis is not running
- Live data if Orchesis proxy is running on `localhost:8080`

## Requirements
- Node.js 16+
- Optional: Orchesis proxy (`pip install orchesis`)
